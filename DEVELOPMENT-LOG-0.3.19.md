# AgentLink 0.3.19 开发日志

## 2026-09-12T21:19:33+08:00 开始修复

- 本机 Codex 根据用户继续解决中断问题的请求，以已发行 0.3.18 ZIP 内源码为基线建立独立工作目录。
- 保留基线 ZIP、原程序和原任务；基线文件与 ZIP 哈希记录在 evidence/baseline.json。
- 范围：技术中断分类、旧错误取消记录的明确恢复、心跳诊断与 UI 一致性、必要的测试隔离。
- 已有诊断证明 A 完成、B 未领取/发送、快照有效；真实心跳失效的具体原因尚未确定，不声称已定位网络故障。
- 本轮使用本机隔离模拟验证，不自动重发真实模型请求或直接修改原场次。

## 2026-09-12T21:26:14+08:00 实现与 B 材料核对

- 新增技术中断错误类型；在线检查保存时间差、节点、实例、具体拒绝原因。UI 时间判断与执行校验对齐，不扩大过期阈值。
- 旧版技术取消仅凭原控制取消=false、固定旧错误及节点错误记录一致性识别；明确继续后关联恢复，原终态不覆盖。主动停止有独立标志，不能恢复。
- 已读 B 提供的原因分析：其本地调用栈指向 artifacts.py:211 解压循环 pump，补齐 A 端原先无法定位的具体阶段。B 报告仍未确认失效因素；本机没有独立读取 B 的原始日志。B 时区 UTC+09 与 A UTC+08 的显示差异本身不代表系统时钟有偏差。
- 修正 context UI 测试的默认生产共享路径，离线回归增加拒绝 UNC/生产数据访问的 Python 审计钩子；它仅保护测试进程，不作为产品安全隔离机制。
- 当前修改尚待验证，无产品包发布。

## 2026-09-12T21:34:42+08:00 定向验证进展

- 首轮 peer-recovery-first.log：9 个新定向用例通过，覆盖时间差诊断、UI/执行时间一致、新/旧中断接续、原始证据保留、主动停止和两端模拟服务重启。
- 真实故障冻结复制件在本机独立目录生成恢复计划：目标 B、索引 1、继承 1 条 A 成果；原始文件哈希未变，零模型请求。只证明恢复计划，未运行真实 B。
- 新增 B 请求可能已发送时的明确恢复验证，纳入正在运行的离线全回归；新增 UI 单测通过，并实际查看了截图，中文、恢复提示和详情正常。
- 暂未发布，等待全回归及包内运行时验证。

## 先前任务中断（时间以保留的原日志为准）

- 21:35 已有内部候选包；21:37 左右包内 peer recovery 和源码 repairs 出现超时。原运行未完成，随后执行工具拒绝访问，留下 VERIFICATION-BLOCKED.md；未发行到 dist。
- 本次读取原对话及已有状态后接续。旧说明不覆盖，原构建归档在 `C:\AgentLink-release-0.3.19\archives\candidate-before-resume`；旧解压包仍为 `package-check`。

## 2026-09-12T23:10+08:00 接续发行核验：冻结与定位

- 执行者：本机 Codex，Windows/Python 3.13.13/PyQt5。本轮不启动真实模型或修改生产任务。
- 建立 candidate-before.zip/json，保存接续前源码；记录并最终核对原 76 个证据文件哈希不变。核对 0.3.18 ZIP 和 87 个基线源码文件。
- 精确复跑原 repairs 与已发送 B 请求恢复用例通过；八次重复诊断仅完成观察到的四次后受工具网络策略中断，保留未完成记录。原超时原因未知。

## 2026-09-12T23:13 至 23:25+08:00 全回归失败与测试修正

- source-regression 首次运行在 intervention 失败；独立重現记录 B 的 NoneType 任务编号错误。测试在 B 未绑定任务时提交指令，修正 test_intervention.py 等待 B 接入，保留去重和目标断言。
- offline_test_guard.py 增加 90 秒起重复线程栈；run_release_checks.py 增加每脚本受控 Job Object、退出/超时回执、确认清理、禁止覆盖旧结果。
- source-final/package-final 的快照断言失败：260 字符路径用普通 glob 不可见，扩展路径证明 source.zip 存在且任务已完成。运行器改为短路径隔离夹具，日志仍存完整证据目录；未修改产品路径实现来掩盖测试问题。
- 接续阶段修改的是上述三个测试文件和交付说明；产品 app 源码沿用此前 0.3.19 实现。具体差异见 resume-changes.json 和 code-vs-0.3.18.diff。

## 2026-09-12T23:33:11+0800 最终验证、封版与发行

- source-release：37 个脚本全部通过；185 个不同具名方法。package-final 的前六项与 package-release 五项合计 11 个不同包内脚本通过，包括原失败的完整脚本。各次首次失败单独保留。
- 冻结事故复制件再次生成 B/index=1/继承 A 一条成果的恢复方案，输入哈希未变，真实模型 0；只证明复制件计划。
- `python -B packaging/build.py --zip-only` 构建；包内 runtime 执行预览和原生 Windows Qt 检查，EXE/源码/GUI 为 0.3.19。已查看主窗和恢复提示截图。
- 封版仅同步最终测试运行器与说明文档，逐文件核对受测代码和 runtime/app/EXE 字节未变；归档原候选后生成最终 ZIP，核对源码、目录、ZIP 和报告副本一致。
- 发行到 `C:\AgentLink-GUI\dist`，包括便携目录、ZIP、完整交付报告、开发日志、证据 ZIP、SHA256SUMS 和 VERIFICATION。精确发布回执见机器 JSON；报告不内嵌自身 ZIP 哈希。
- 状态：本机交付完成；真实 SMB、B 独立验证、双机模型故障恢复、生产升级、耐久与完整验收仍未执行。没有向其他人发送材料。
- 完整日志在 `C:\AgentLink-release-0.3.19\evidence\resume-20260912-231017`；分发证据索引见 AgentLink-0.3.19-Verification-Evidence.zip/INDEX.md。原场次及快照原件只留本机。
