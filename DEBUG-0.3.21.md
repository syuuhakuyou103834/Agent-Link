# AgentLink 0.3.21 升级与诊断

双方退出旧程序，分别解压0.3.21到新目录，运行AgentLink.exe，沿用原本机设置和共享目录。不要删除历史、快照或本机账号资料，也不要为升级而在任务上点“停止”。两端版本必须一致。

连接后先显示确认中。约1秒发送一次心跳，并回应对端随机挑战；双方就绪后才允许正式执行。对端时间领先或落后不会单独导致中断。显示的系统时间差不是测得的真实时钟偏差。

12～不足30秒无有效心跳更新：响应延迟，暂停新请求/实时补充/交接推进，继续接收已发送请求的已有结果；重新确认同一实例后可接续这段短等待。30秒持续无响应、明确退出或归属改变：保留结果和请求记录，进入技术中断，恢复需要明确“继续”。

对0.3.20的B接收快照中断：两端升级就绪后，选择原对话输入“继续”。A已完成成果不重跑；关联恢复验证原快照和账本后继续B未完成步骤。若B有已发送记录或证据缺失，按uncertain/UNKNOWN处理，不自动重试。用户主动停止的任务不会复活。

详细记录包含liveness_state、local_silence_seconds、peer_sequence、challenge_nonce、freshness_basis、实际执行方和request_state。共享IO错误单独显示，不当作钟差。GUI节点就绪含身份及角色锁检查。

历史上下文预算阻塞在握手期间收到明确继续，会保留该指令等待；不需要连续重复点击或重新跑A。若提示旧协议、文件权限、模型额度或快照校验失败，应按具体错误处理，不删除校验文件来绕过。

真实SMB、双机、模型和睡眠唤醒验收状态见RELEASE-0.3.21.md。本工具环境的权限预检失败不等于用户电脑必然同样失败；不得通过放宽模型权限规避。
