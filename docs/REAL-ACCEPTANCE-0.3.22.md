# 0.3.22 双机候选包验收记录

本表供真实 A/B 环境填写，未填写不表示通过。使用专用测试任务和测试目录，记录两端版本、机器、运行时、包 SHA256、任务及 Codex 任务 ID；不对正在进行的生产任务注入故障。

| 场景 | 预期 | 实际状态/证据 |
|---|---|---|
| 两端原生启动与正常 A→B→A | 版本一致、B 独立接收核验并测试、A 汇总 | 未执行 |
| A 初审完成后 B 正常退出并重开 | 保留 A 成果；明确恢复只承接未完成步骤 | 未执行 |
| 专用任务中受控终止 B | 显示真实错误原因、记录锁/实例变化；不自动重发 | 未执行 |
| 专用 SMB 通道临时不可用 | 连续失败计时不被普通轮询清零，保护生效 | 未执行 |
| 网络恢复、双方重新就绪 | 历史故障仍保留；明确恢复后建立可追踪恢复链 | 未执行 |
| 睡眠/唤醒和系统时钟偏差 | 重新确认执行资格，不仅根据墙上时间判定 | 未执行 |
| 原请求已发送但结果未核实 | 不自动重发，核查已有结果和文件后明确处理 | 未执行 |
| 用户停止后点击恢复或输入继续执行 | 不复活已停止任务 | 未执行 |
| 只读审查和独立测试权限 | 源码不变，测试写专用目录，不能访问未授予路径 | 未执行 |
| 原始源码 ZIP 与 B 证据回传 | 分别核验指定原包与回执；A 实际接收 B 原始证据 | 未执行 |
| 0.3.21 记录升级与回退 | 原记录和成果保留，两端版本一致，无隐式模型调用 | 未执行 |

测试结果使用“通过/失败/阻塞/未执行”；保留首次失败和后续成功，附命令、时间、哈希和日志路径。真实模型请求数单独记账，不能用界面对话轮数代替。

如果安全软件关闭 B，仅在取得对应安全软件事件/隔离记录后归因；不要关闭防护或添加整盘排除来取得通过结果。
