# AgentLink 0.3.17 开发日志

## 2026-09-12T02:22:23+08:00 基线与需求

- 执行者：A 本机 Codex；基线为已交付 0.3.16/source，492 个文件已记录 SHA-256。
- 用户授权实施 BR-017-01–27；遵循 docs/BUSINESS-REQUIREMENTS.md 和 RELEASE-PROCESS.md。
- 工作副本：C:\AgentLink-release-0.3.17\source；旧发布和审查快照保留。
- 初始检查：服务位于 app/engine.py，不存在 app/service.py；一次只读路径查询失败，未影响文件。
- 下一步：实现持久任务澄清、确认与单一界面，验证后打包。真实 B/SMB/模型验收尚未执行。

## 2026-09-12T02:23:48+08:00 基线核对

- 发布目录后续留下 432 个 A-verification/test-output 文件；与冻结 ZIP 中 60 个源码文件核对，源码内容均一致。
- 工作副本的额外文件移入 evidence/inherited-extra-files 保留，构建只使用冻结 ZIP 对应源码。未修改旧版本目录。

## 2026-09-12T02:35:19+08:00 实现统一对话初版

- BR-017-01–27：新增 app/conversation.py 持久对话、只读澄清、结构化任务说明、明确确认、正式步骤调度及请求台账。
- 新增 ui_conversation.py 单一时间线，移除主窗口模式/项目表单/轮数控件/单独节点输入页；旧记录保留查看导出。
- review_workflow.py 按确认行为分别配置 A 的修改、只读审查或仅讨论权限。B 始终源码只读。
- 恢复继续使用独立子场次并保留原失败记录。初版待回归，尚未声称实现验收通过。

## 2026-09-12T02:47:51+08:00 首轮测试与修订

- 新增集成模拟 11 项，首轮 10 通过、1 失败：B 补审测试在命令尚未提交时判定队列为空，修正等待实际请求到达后验证。首次失败保存在 unified-first.log。
- 修复产品：主动关闭 App Server 后不再轮询已关闭管道；只读对话流式写盘限流，并避免无变化时重复发布整个对话，复测由约 263 秒降到约 68 秒。
- 第二轮新流程 11 项均通过；新增确认摘要绑定，提前或过期的“开始”不能批准新说明。
- GUI 首测失败为 HTML 空格折叠导致预期字符串不符；修正断言后，代码审查 GUI 已通过，文本 GUI 等完整回归。
- 原测试中“B可发起”与 BR-017-04 冲突：改验 B 拒绝发起；恢复测试改由 A 调度、仍恢复 B 未完成步骤。旧脚本保存在 tests/legacy-0.3.16，其他清理/历史/权限断言保留。
- 本机真实 Codex 只读/审查/实施三种权限配置预检均通过，0 次 turn/start；未执行真实模型或工具权限边界测试。

## 2026-09-12T02:53:55+08:00 边界审查与兼容回归

- 发现并修复：暂停需要同时阻止预检后的请求发送，主输入框“继续”在控制队列直接解除暂停，避免等待模型线程造成死锁。
- 补齐 A 在停止后重新澄清、B 不复活停止任务、B 自动显示当前任务、消息保存回执后才清空输入框。
- 新增 8 项记录与状态守卫均通过；新流程扩展到 14 个用例。
- 旧并发测试假定 B 也进入准入屏障，与新权限契约不符；保留同时提交屏障，B 在入口拒绝，A 仍只产生一场任务。
- 旧恢复测试的 replacement(B).start 改为 A 调度，原 B 重启后恢复会话和结果继承断言保留。所有首次失败保存在回归目录。

## 2026-09-12T02:55:43+08:00 确认并修复锁文件创建竞争

- 最终新增集成集首次 13/14 通过，1 项在节点初始化阶段失败。原日志 unified-final.log 保留。
- 原因证据：test-output/21b9a953a0d047ba84acc030e4e126bb/B/logs/agentlink-errors.jsonl 显示 storage._json_guard 在加锁前 handle.flush 发生 PermissionError errno=13。不是模型额度或测试期望错误。
- 修复：app/storage.py 先对空文件取得字节锁，再由持锁句柄初始化；原稳定侧文件不删除。现场验证 Windows 可以锁定空文件范围，并拒绝第二句柄。
- 新测试重现旧顺序 errno13，并用 12 次、每次 6 个并发写者检查首次锁文件初始化。请求不会因存储重试自动重复。
- 此修复属于本版持久对话依赖的共享存储路径；带此修复进行最终回归。

## 2026-09-12T03:05:00+08:00 最终回归与交付准备

- A 本机：31 个回归脚本全量运行结束，29 个成功，2 个夹具失败；未覆盖原结果。
- GUI 失败证据表明 B 的补充消息根本未提交，原因是刚选择历史时按钮仍在等待回执；测试改为等同一对话且发送可用。文字/代码 GUI 均复跑通过。
- 空文件锁测试先追加了数据，第一句柄追加落在锁外，因此未产生预期错误。改为另一句柄锁住空文件的首字节，验证无锁初始化被拒绝；与 12×6 并发守卫用例一起通过（8/8）。这不等同于完整复现原现场的两个创建者时序。
- 截图发现历史重复显示内部场次，修复 engine.history 的过滤；两个 GUI 用例断言一条对话且内部场次仍在详情。
- 完成最终 31 个脚本的结果核对，新增 14 项流程、8 项守卫；不把复跑累加为新增验收。首次失败保留 final-regression 等目录。
- 一次截图读取选中了尚未生成截图的正在运行目录，工具报文件不存在；改读完成截图，未因此改程序。
- 构建脚本增加源码内报告/日志副本，确保从随包 source 再构建时文件齐全；BUILDING 版本更新为 0.3.17。
- 下一步：构建便携包，用包内 Python/Qt 和隔离数据验证，再生成哈希和发布回执。无真实模型任务、B 操作或生产数据修改。

## 2026-09-12T03:11:41+08:00 发布包验证与封存

- A 本机：由冻结 0.3.16 基线构建 0.3.17，嵌入 Python 3.13/PyQt；ZIP 约 23.3 MiB。构建命令与输出见 build.log。
- 包内运行时的版本、隔离 main.py 预览、Windows 原生插件、完整代码 GUI、新流程 14 项集成共 5 组检查全部通过；最后 14 项耗时约 95 秒，0 次真实模型请求。
- GUI 截图人工查看：单时间线、单输入、可展开证据、历史无内部场次重复、版本标识正常；测试数据为模拟数据。
- 核对包中程序/测试/构建代码与受测工作副本一致；EXE FileVersion/ProductVersion 均为 0.3.17.0。
- 0.3.16 冻结 ZIP 的 60 个源码文件及其旧发布目录仍与基线清单一致。原审查快照未操作。
- 最终脚本记录 31 个脚本通过，日志识别出 159 个不同类/方法键；GUI/故障子断言不折算为新增编号。源码新流程 14 项台账与重复运行分开索引。
- 真实 Codex 三种配置预检通过，但真实工具边界、真实 SMB、B 独立验收和双机真实模型未执行。安装器未构建，默认生产数据 EXE 启动未执行。
- 文档补丁有一次因上下文行不匹配而未应用；重新定位后写入并核对，未影响代码。模拟台账首次按模块前缀计成 15 项，规范为类/方法后为 14 项；不重复计数。
- 下一步：向新 dist 版本目录复制并逐文件校验；回执和 ZIP 最终哈希放在外部 VERIFICATION/SHA256SUMS，避免自身循环依赖。

## 2026-09-12T03:11:41+08:00 发布目录就位

- 已复制到 `C:\AgentLink-GUI\dist\AgentLink-GUI-0.3.17`，初次复制逐文件校验一致，未覆盖旧版本。
- 当前报告和完整开发日志同步置于 dist 顶层、便携根及 source；下列封存过程校验目录/ZIP和独立证据清单，最终结果见同级 `VERIFICATION-0.3.17.json`。
- 此次交付没有上传 GitHub、写入 B 的共享发布目录或运行用户生产任务。B 独立验收仍未执行。
