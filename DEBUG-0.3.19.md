# 0.3.19 中断、恢复与诊断说明

## 对这次 0.3.18 记录的处理

1. 保留原任务和原源码目录，不删除记录、不点“停止任务”来升级。
2. 确认当前没有模型或测试继续执行后，退出 A、B 两端旧程序。分别解压 0.3.19 到新目录并运行 AgentLink.exe，保留原数据目录和共享目录设置。
3. 两端连接后，在 A 端选择原来的中断对话，输入 `继续`。不要新建同样的审查任务，也不要把原任务目录改成 0.3.19 源码；原场次审查的仍是 0.3.18。
4. 程序验证旧技术取消的证据，继承 A 已完成的成果与快照，建立关联恢复场次，从 B 未完成步骤继续。升级或重连本身不会重发请求。
5. 如果软件显示用户主动停止、证据不匹配、已有恢复子场次、范围不一致或进程清理未完成，应保留错误再核查，不编辑 state.json、删除 claim 或角色锁文件。

这是交付后的真实双机操作步骤。本机只验证了隔离模拟及真实记录复制件，不代表已经恢复原任务或完成真实 B 审查。

## 心跳错误代表什么

心跳记录无效会暂停后续步骤。错误现在记录观察节点、对端节点、失败分支、本机观察时间、对端更新时间和差值、实例、阶段及请求状态。数据可在“展开详情 → 任务与快照”以及该场次 state.json / 节点错误记录中查看。

- `peer_heartbeat_stale`：心跳时间差达到 12 秒；可能是更新延迟、共享 I/O 阻塞或时钟偏差，不直接等于进程退出。
- `peer_clock_ahead`：对端时间领先超过 1 秒；应核对系统时间同步。时区显示不同本身不等于时钟偏差。
- `peer_offline`：对端状态离线或缺失。
- `peer_timestamp_invalid` / `peer_role_invalid` / `peer_instance_invalid`：记录字段无效。
- `peer_instance_changed`：本场绑定的对端实例已替换；旧场次不会直接交给新实例，只能明确关联恢复。

顶栏在线徽标的时间有效性与执行检查对齐；开始执行仍单独核对实例和角色锁。守卫阈值及角色锁保护没有删除。心跳记录新增实际发布间隔，方便定位耗时阶段；本版没有声称已找到此前瞬时失效的唯一根因。

## 请求状态与继续

- `not_sent`：没有该步骤的 claim 和调用台账，未启动模型；明确继续会开始该步骤。
- `claimed_preflight`：已领取但未记录模型发送，仍需核对原范围；不能当作完成。
- `send_pending` / `interrupted_uncertain`：可能已经发送，不能自动重发；明确继续承接原本机任务并先核对已有操作。
- `result_received`：模型返回已收到；交付尚未完成时需要核查，不能据它宣称已完成 B 审查。

主动停止的任务不会复活。新问题可以在结束后输入，但单独“继续”不会被误当成一个全新任务。

## 本机复测

`python tests/run_release_checks.py` 使用本机短路径测试共享目录，并通过 Python 审计钩子拒绝测试进程访问 UNC 或生产数据目录。默认执行除历史耐久脚本外的全部 `test_*.py`；`--names test_peer_recovery.py` 可定向验证。`--out` 必须是尚不存在的证据目录，每个脚本默认 240 秒上限，90 秒起输出超时线程栈，并用 Windows Job Object 清理本次测试子进程。环境、命令、退出码和清理状态保存在 JSON 回执中；首次失败不会被后续运行覆盖。

该钩子不是产品权限机制，也不保证任意第三方子进程的行为；现有 App Server 模拟程序仅接收测试目录。日志目录可以较深，夹具数据保持短路径，避免普通 Win32 路径查询将实际存在的深层快照误报为缺失。

`python tests/replay_peer_incident.py <冻结证据目录> <新的本机输出目录>` 只从复制件生成恢复方案，不启动服务或模型。不要对正在使用的共享目录执行原始 0.3.18 的 `test_context_ui.py`：它使用默认共享目录的问题已在本版测试中修正，旧版本原件为证据仍保留。
