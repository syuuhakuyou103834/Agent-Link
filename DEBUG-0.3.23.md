# AgentLink 0.3.23 使用与诊断

两端均换用独立解压的 0.3.23，保留原版本与任务数据。先核查当前请求、正常退出，再启动新目录 AgentLink.exe；不要为了升级点击“停止任务”或删除锁。用户明确停止的任务不会复活。

## 结果与恢复

选择原中断任务，核对已有结果，明确点击“恢复任务”或输入“继续”。项目、正式文本、澄清/补充聊天共享结果验证。已返回有效结果优先在本机完成验证/发布，新增模型请求单独记录；原结果未验证不等于正式成果。不确定请求仍需用户核查，不能自动重发。

数据目录 received-results 下保留缓存。新格式 result-v2 包含小型 receipt.json 和独立 body.json；正文容量为 64 MiB，回执限制 64 KiB，按 UTF-8 实际字节计。读取正文按块计算摘要，随后在受控容量内解析。它不是无限内存或无限正文支持。

旧版 result.json 在 64 MiB 文件容量内校验读取，原字节不重写。旧版无法直接使用新格式，不承诺新任务可用旧程序续跑。null、损坏、错归属、未提交目录、已返回但缺失等状态阻塞新执行。超出正文容量时保留未提交原件并说明原因，不要手动删除它来解除保护。

模型已返回不等于共享结果已经发布；有效旧缓存复用时记录 reused-步骤.json，new_model_requests=0。恢复时未发送的补充继续保留，不能冒充已经交给原请求处理。

## 导出和日志

完整记录/实施交接在独立写入线程原子保存，保留 UTF-8 BOM。界面先显示“正在保存”，实际完成才显示“已保存”。失败保留原目标；关闭窗口时会等待已接受的导出完成，磁盘不可用时需核查保存错误。

通常本机日志位于 %LOCALAPPDATA%\AgentLinkGUI\logs：

- runtime-events.jsonl / runtime-state.json：原有元数据诊断；有界队列及轮转。
- agentlink-errors.jsonl：单独的详细错误通道，异步保存；4 MiB 轮转、四个备份、64 条队列。message/traceback 超过 65,536 字符会截断并记录对应 truncated 标记；仅限本机诊断，不自动公开。
- 详细日志失败或队列丢弃会返回独立健康状态，不能用结构化事件写入正常掩盖详细日志失败。磁盘满时不能保证持久化，界面仍保留业务错误并显示保存失败计数。

缓存损坏、日志失败和通信故障是不同问题。不得从“日志没有记录”推断“模型没有收到请求”。本版不默认抓包、上传或唤醒 Codex。

## 手动核验 A/B 证据包

将发送方指定原 ZIP 复制到接收机，保留其文件名及原始字节，并从发送方记录取得 SHA-256。在便携目录的命令行运行：

```text
Verify-Evidence.cmd --archive "C:\Evidence\B-evidence.zip" --sha256 "发送方的64位SHA256" --out "C:\Evidence\receipt-new" --conversation "实际会话编号" --job "实际运行编号" --step "001-B" --sender B --receiver A
```

输出目录必须尚不存在。工具验证同一打开文件的原 ZIP SHA、条目 CRC 和逐文件 SHA，不解压、不执行附件；拒绝重复/不安全/加密条目及超出受控容量的包。限 100,000 条目、合计 512 MiB 解压尺寸。

receipt.json 为小型归属回执，files-index.jsonl 为逐文件清单，状态 hash_verified 仅表示本机文件核验；agent_read 和 tests_executed 仍为 false。它不自动回传文件、不自动让 A/B 阅读，也不替代独立复核。双方分别保存并交付自己的回执，真实接收状态见发行报告。

## 项目恢复的关联证明

项目结果复用须匹配源码清单、阶段、修订和任务权限摘要。0.3.22 旧缓存没有此证明，虽可校验读取，也不能自动发布到当前项目；需人工核查。原始答案先保存，随后关联证明写入失败时同样保留原件并阻塞自动发布。已提交聊天消息的恢复不会重复追加同一消息。
