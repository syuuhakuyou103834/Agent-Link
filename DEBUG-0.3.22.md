# AgentLink 0.3.22 使用与诊断

0.3.22 修复执行清理、持续心跳 I/O 故障、证据索引和发布暂存问题。完整验收状态见 RELEASE-0.3.22.md；本机模拟通过不能替代真实双机、模型权限和现场恢复。

## 升级和恢复

1. 保留两端旧程序、任务记录和源码快照。当前任务仍在执行时先等待其完成，或在核查并保存已有结果后退出；不要为了升级点击“停止任务”，因为明确停止的任务不允许复活。
2. 两端均解压 0.3.22 到新目录，启动各自的 AgentLink.exe。沿用各自账号、设置和共享目录，不向对方复制登录资料。旧版本可以查阅历史，但不能与本版混合执行新步骤。
3. 选择原中断任务，核查已保存的 A/B 成果和请求状态。确认双方就绪后点击“恢复任务”，或明确输入“继续”“继续执行”。程序承接未完成步骤；升级本身不发送模型请求。
4. “节点就绪”只描述当前连接和执行资格，不代表历史失败已经解除。“中断，等待恢复”需要上述明确操作；模型回复“收到，继续”不等于程序完成恢复。
5. 如果显示“清理待处理”，先重试“断开”。清理成功后重新连接，再核查并恢复。旧子进程未确认退出时，程序保留执行锁并禁止新执行。不要删除锁文件或并行开另一窗口绕过它。
6. 原请求状态不确定时，先查原 Codex 任务、已保存结果和文件变化，再明确决定是否继续。本版不会自动重发未知结果的请求；“未发送”“已写入管道”“结果已收到”“成果已发布”是不同状态。

本版采用新的执行能力标识，双方必须同时升级。旧记录保持原结构并按已有恢复机制关联；对已验证的旧证据包增加独立提交记录，缺失、null 或损坏的已有索引不会静默重建。程序回退应使用保留的旧目录，两端同时回退；不要在运行中覆盖程序，也不要为回退删除新记录。生产数据回退是否通过以发行报告为准。

## 本机诊断文件

通常位于 `%LOCALAPPDATA%\AgentLinkGUI\logs`，指定独立数据目录启动时位于该目录的 `logs`。

| 文件 | 用途 |
|---|---|
| runtime-events.jsonl | 状态变化、心跳观测、请求写入和结果、清理事件；4 MiB 轮转，最多保留四份旧文件 |
| runtime-state.json | 最近一条诊断事件及写入失败、丢失事件等计数；不是完整任务数据库 |
| agentlink-errors.jsonl | 原有详细错误记录，可能含本机路径，分享前检查内容 |
| app-server-*.jsonl | 原有底层接口记录；可能含任务内容，不默认对外上传 |
| 数据目录/execution-cleanup.json | 清理失败及确认记录，包含进程身份；不是可手工清除的“解锁按钮” |

可在 PowerShell 使用 `Get-Content -LiteralPath '<实际路径>\runtime-events.jsonl' -Tail 30 -Wait` 观察增量。此方式不会唤醒 Codex，也不会自动处理告警。

新结构化诊断只接受预定元数据字段：节点/实例、运行与会话标识、状态、原因码、心跳序列和引擎验证结果、锁、清理及请求状态。默认不写完整提示词、源码或凭据，不启用网络抓包、上传和自动重试。

写盘由有界队列和独立线程完成；磁盘慢或拒绝访问不阻塞心跳和界面。队列满时会丢弃诊断事件并计数。若 `dropped_events` 或 `write_failures` 非零，诊断时间线可能不完整；不能把“日志没有事件”推断为“未发送请求”。

心跳观测来自本机经过时间、序列号和实例挑战。12 秒/30 秒保护语义保留，持续 I/O 故障分别按通道累计。单条在线文件或墙上时钟时间差都不足以证明执行资格。

## 源码交付核验

B 的展开源码目录不增加额外文件，仍可按清单检查。其旁边 `.received/<快照摘要>/` 有一份明确保留的原始 source.zip、manifest.json、ready.json 和 delivery.json；B 的模型只获该快照资料目录的读权限。

delivery.json 记录服务完成的原始 ZIP 核验，不代表 agent 已阅读或测试。B 应分别报告原包接收、哈希、实际阅读范围和运行证据；A 仍需取得并校验 B 的原始报告与测试证据。自动回传任意 B 测试附件尚未提供。

若两个 ZIP 哈希不同，先核对它们是不是同一个指定交付包。文件内容清单相同不代表重新压缩的 ZIP 字节相同；重新打包的本地 ZIP 不能替代指定原包的接收证明。
