# AgentLink 0.3.20 升级、测试与故障说明

两端均需 0.3.20。新执行能力标识为 agentlink-execution-lease-v5；旧端会在创建/领取前被拒绝，不能用只升级一端测试正式协作。旧版源码和发行目录保留。

升级前保存设置和历史的一致备份，在当前步骤结束或已中断后退出两端程序。将本版解压到独立目录，运行 AgentLink.exe，检查原生标题为 0.3.20。不要为升级点击“停止任务”：主动停止仍然不可恢复。升级不触发模型请求。

旧历史仍可查看。对技术中断选择原对话并明确输入“继续”；继承已完成结果。旧 included_next_request 或已经发送但不确定的消息不会被批量改回 queued 自动重发，先核查原记录。旧程序直接接管本版未完成状态的回退未验收；需要回退时使用升级前一致备份，不能只替换 EXE 后继续同一个活动任务。

创建失败：本版在对话中保存创建意图，正式提交前不调度。证明没有 claim/call 凭据的中间态可以终止并重新准备；有请求凭据则明确阻塞，不自动重放。停止与暂停在最终关联处重新检查；实际 turn/start 的管道写入与停止提交互斥，等待模型回复时不持有共享状态锁。

补充消息：候选 prompt 不消费 queued 输入。请求账本的 input_ids/input_sha256 一次性记录本次消息集合；预检拒绝时保持 queued，已提交但未确认的输入显示 uncertain，结果确认后显示 delivered。查询状态以账本投影为准，不能只读取 input 文件内旧的 queued 字段断言“未发送”。代码、统一文本和兼容文字工作流使用相同提交机制。

权限：discuss 所有阶段只读；review 允许独立测试目录写入，源码只读；edit 仅 A 实施阶段允许授权源码写入。最终总结只读。权限配置与实际 OS 工具执行是不同证据层级，见发行报告的未验证项。

长路径：路径前缀、枚举和相关读写入口统一处理，包含短父目录下长子路径。正式项目仍限制为本机具体目录；UNC 用于共享通信，不能以长路径修复扩大项目目录授权。

快照清理：成功接收删除本次 payload.zip 和空暂存目录。失败时保留包含最多 2048 字符错误的 failure.json 和快照标识，尽可能清理本次重复包/未发布树；原共享包和其他任务不受影响。文件被占用或权限拒绝时记录日志并保留残留，清理失败不掩盖已发布快照。诊断元数据持续保留，未设置跨任务自动删除策略。

离线测试示例（选择新的输出目录，重复执行不得覆盖旧证据）：

```powershell
python -B tests/run_release_checks.py --out C:\AgentLink-tests\020-run-001
```

测试日志、临时数据、模拟通信和夹具输出都写该目录。AGENTLINK_TEST_OUTPUT_ROOT 控制独立脚本的输出；标准 runner 同时设置 TEMP/TMP 和写入边界守卫。legacy-0.3.16 是历史测试材料，不由标准 runner 递归执行。本机模拟不代表真实 SMB、双机或真实模型验收。
