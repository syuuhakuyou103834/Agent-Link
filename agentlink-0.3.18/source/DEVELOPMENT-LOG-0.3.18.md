# AgentLink 0.3.18 开发日志

## 2026-09-12T17:59:43.613884+08:00 基线与范围

- A 本机，用户授权修复上下文超限并发行 0.3.18。此前两次中断未留下工作副本或发布文件。
- 从冻结 0.3.17 ZIP 提取 81 个源文件，未复制后续用户测试产物；原版本及诊断冻结记录保留。
- 实施输入投影、只读证据索引、预算错误、明确续接和大记录回归；不顺带修复审查清单中无关问题。

## 2026-09-12 18:17 +08:00 实现与首批验证记录

- 执行者：A 本机 Codex；Windows、Python 3.13、PyQt5。以下是本机工程记录，不是 B 独立审查。
- BR-018 / app/context_view.py：从共享完整记录生成发送视图；消息正文与来源保留；turn 的重复正文及工具事件保存为本机文件，生成规范化 SHA-256 索引；只追加该证据目录的只读权限。较早普通答复可以转为明确原文引用，用户要求、pending、blocked/changes_requested 审查及最近消息保留。未生成模型摘要。
- app/context.py、conversation.py、review_workflow.py：预算错误保存阶段、字符和 JSON 字节数；统一聊天、正式文字任务、正式源码任务在请求前检查，正式 claim 移到最终预算检查之后。GUI 详情显示回执、证据索引和超限状态；原时间线及导出正文不改写。
- 恢复：识别 0.3.17 的预发送上下文错误；明确继续不会用“继续”替换原 B 补充。升级后旧实例不能执行旧任务，持执行锁检查后建立关联恢复，继承已完成步骤。旧版本能力检查失败也保留原未处理消息。完成/停止记录不自动复活。
- 用户原始冻结记录通过 tests/replay_context_fixture.py 回放：319,519 字符 → 14,048 字符；完整 B 请求 14,810 字符 / 26,718 JSON 字节；16 条消息正文完全保留，26 条工具记录可按文件索引读取。真实模型请求 0，原 SHA-256 仍为 61127aeaf0cff2e3f9bd635cd0eaa50794de8176d1d2aba7346f924b631c16ac。原件及回放私有目录不随公开证据包交付，公开包仅收 metrics.json。
- 首次失败（完整原始输出保留在 evidence/）：first-conversation-tests.txt 的一个源码用例超时；原因是 mock_server.py 把“之前结果”标题当 JSON 结束符，去掉重复结果后夹具无法解析。改用 JSONDecoder.raw_decode，与生产 RPC 无关。其余 13 项通过；后续完整回归 14 项通过。
- first-context-view-tests.txt：外置用例给了 150,000 字符，低于 180,000 软阈值，错误预期发生外置。修正为 210,000 字符，6 项后续全过；未调整产品阈值迎合测试。
- first-context-workflow-tests.txt：6 项通过，旧版本模拟因断开 B 清掉能力记录而 KeyError；改为保留在线角色锁、只移除新能力，7 项后续全过。覆盖文本/代码发送前零 claim、B 原消息、两端重启和 A/B 任一节点继续。
- context-resume-guard.log：新增保护测试使用非法假场次编号，触发现有校验；修正夹具为合法格式后 context-resume-guard-final.log 通过，原消息在两次不兼容恢复请求后仍保留。
- tests/check_unified_preflight.py：本机已安装 Codex 的 summary/review/implement 三种权限配置均接受额外只读证据目录；仅创建空线程，未发送 turn/start，未实际执行文件工具，不能据此宣称 B 权限隔离已验收。
- 全量 tests/run_release_checks.py 正在独立 test-output 目录运行；打包 packaging/build.py --zip-only 已生成内部候选包。候选包内报告暂为构建草稿，正式发布前补齐结果，未发布到 C:\AgentLink-GUI\dist。
- verify_package.py 正在使用 ZIP 解包后自带运行时复跑启动、GUI、普通流程及上下文回归。未启动生产讨论、未操作 B 电脑、未上传 GitHub；保留原版本及快照。

## 2026-09-12 18:24 +08:00 全部回归与包内验证完成

- 完整回归 `python tests/run_release_checks.py`：34 个脚本全部通过，输出 `evidence/20260912-181145`。另补 `test_context_resume_guard.py` 1 个脚本通过；最终共 35 个不同脚本、174 个不同类/方法键，新增上下文 15 个用例。机械方法计数不等于业务验收数。
- 包内 `verify_package.py`：版本、隔离 main.py 预览、Windows Qt 插件、源码审查 GUI、原对话流程 14 例、新投影视图 6 例、新上下文流程 7 例及 UI 1 例均通过。`verify_package_additional.py` 使用同一包内运行时补测恢复守卫及最终 UI 字体。
- 视觉检查发现首张 context-blocked.png 没有文字；offscreen 新夹具没有加载字体，功能断言虽然通过但截图不可用。参考原 ui017_harness 加载 Windows 微软雅黑字体，源码与包内各复跑一次；修正后截图已实际查看，超限数字、详情、完整正文和继续提示可见。初次图与日志一同保留；没有因此改动产品 UI 逻辑。
- 包内检查总计 9 类，UI 字体另复跑一次；重复运行不增加独立用例数。EXE 元数据及 GUI/源码标识均为 0.3.18。
- `collect_evidence.py` 收集完整回归、首轮失败、定向复测、7 个新流程的模拟请求台账、GUI 和原失败回放 metrics。选择合成数据与测量结果，不交付真实对话正文或本机 Codex 私有 RPC。
- 报告、README、DEBUG 和 BR-018 已补齐；保留“不无限上下文、原单条工具捕获限制、共享 JSON 上限、证据增长、真实双机及权限未验收”的边界。未删除旧业务功能，未顺带修改其他审查缺陷。
- 最终再打包只同步报告、完整日志和最后的测试夹具；产品代码和包内运行时随后逐文件与已验证副本比对。将通过全新 dist 目录发布，不覆盖已交付旧版本。

## 2026-09-12T18:26:05+08:00 发行目录就位与封存

- 已复制到 `C:\AgentLink-GUI\dist\AgentLink-GUI-0.3.18`，初次逐文件哈希一致，原 0.3.17 目录 81 个基线文件及 ZIP 校验通过；原失败 JSON 哈希未变。
- 交付源码/测试/构建文件与本机受测副本及解包运行时核对一致；运行时逐文件一致。最终文档和新增两个诊断测试夹具分别经过复核，未修改已通过回归的产品代码。
- 报告、说明、开发日志、源码和测试一同发行；校验清单、外部 VERIFICATION 及独立证据 ZIP 记录下面封存步骤的最终结果，不把自身 ZIP 哈希嵌入 ZIP。
- 此次没有上传 GitHub，没有操作 B 电脑或生产讨论。B/SMB/真实模型/生产升级/耐久验收保持未执行。
