# B 第三轮意见收束与 0.3.6 补充验收

本轮保持 **0.3.6 产品源码和原 ZIP 完全不变**，完成剩余本机补证：新增 5 个执行权窗口通过；冻结源码正常耐久 110 场、330 次模拟请求通过，并逐步骤双向核对。未发现需要新增产品修复的确定缺陷，不生成新版本。原 59 项回归仍为上轮执行记录；新增 5 项不能被描述为本轮重新执行全部 64 项。

## 四项意见的处理

| 意见 | 结果与限定 |
|---|---|
| 角色锁与执行权 | 新增 5 个窗口：对端心跳过期、对端角色锁丢失、对端实例更换、旧执行者角色锁丢失、旧执行者实例更换。固定旧模型夹具请求在运行，竞争者先通过兼容检查，再因讨论锁冲突被拒绝。显式停止提交后、旧请求仍被屏障保持时也不开放讨论锁。释放旧响应后终态不变、无后续步骤、无额外请求；旧工作线程退出后锁可重新取得。 |
| 最后检查后退出 | 直接关联上轮已有通过用例，不增加检查次数来承诺消除窗口。最后检查后可能已发 1 次；没有可靠本机完成结果时按结果不确定处理，禁止自动重发。已保存本机结果但共享发布失败时保留相同结果和失败状态，禁止重调模型。 |
| 同版本合法对照 | 本轮耐久中分别抽取第一场 A→B→A 和 B→A→B，各 3 次模型夹具请求。全部 110 场也逐场核对。详见 same-version-controls.json，不能用混版拒绝测试代替此正常对照。 |
| 最新候选耐久 | 在源码哈希与原 ZIP 一致的 0.3.6 冻结副本复跑原基线，110 场/330 请求通过。旧版正常耐久仍留在旧报告，未转记为本次结果。 |

角色锁用于节点实例占用，discussion.lease 用于整场讨论准入；二者作用不同。新增测试证明上述本机受控窗口没有因角色锁或心跳变化释放仍被持有的讨论锁。使用的是进程内阻塞模型夹具、实际本机文件锁及真实发起服务循环；**不是硬杀 GUI/执行服务/进程树，也不证明 SMB 丢锁语义**。迟到写入被显式终态阻止，不将此说成没有终态隔离的任意孤儿进程都已被验证安全。

## 执行证据与测试夹具修正

最终新增用例日志：`evidence/212923-authority.log`，5 项通过；每个故障场 1 次已接受夹具请求，竞争者 0 次，记录在 `authority-summary.json` 及各自 fixture-requests.jsonl。

初次 3 项因模拟服务心跳仍为 offline，在兼容检查提前报错，尚未触及目标讨论锁窗口；日志 `212744-authority.log` 保留。修正测试夹具心跳为 running 后通过，另补旧执行者两个窗口。此修正仅改新增测试，未改产品代码，不能记为 0.3.6 产品缺陷修复。

现有恢复证据按具体用例关联在 TRACEABILITY.json，并对引用文件保存 SHA-256。对用户的恢复说明是：失败后先查看该场次本机完整结果；有已保存结果则保留并核查共享发布情况，没有可靠完成结果则结果不确定、不可自动重发。当前没有承诺故障后自动补发共享结果；也没有把界面的普通 failed 标签说成新增了专门的“不确定”状态。真实断共享或进程硬崩溃时此说明和诊断的完整性仍待验收。

## 耐久统计与合法对照

原基线不变：预热 10 场、测量 100 场，中文议题长度和 0.002 秒流速固定，单轮交替由 A/B 发起。资源范围是含两个 NodeService 线程的宿主进程加两个 mock 子进程；首 20 个测量场 samples[10:30] 与末 20 场 samples[90:110] 取中位数，增长阈值 20%。

- 私有内存增长：4.61%；句柄增长：0.00%。
- 每个预期步骤恰好匹配一条请求；反向检查所有 330 条请求均被使用且没有复用。核对场次、步骤、节点、thread/turn ID、请求行、输入文本哈希，发起方初稿先于修订。
- 110 场全部 completed，节点顺序正确；结束后所有本次已记录 mock 子进程退出。
- 原始运行目录：`source/test-output/end-ac7b0837`。
- A 正常对照场次：`20260910-132602-0652d052f3d847258ca6d9927fc5db92`；B 正常对照场次：`20260910-132604-4d155dcabece42759730983d37c96aa8`，各 3 次请求，具体台账行见 `same-version-controls.json`。

这是**本机两个服务、两个模拟子进程**的合法流程与耐久证据，不能将 T03/T04 的真实双机账号模型验收改判通过。

## 协议、混版及统计范围

兼容检查依据协议和能力字段 `agentlink-live-instance-v2`，以及对应实例/角色锁/心跳绑定，不是只比较程序版本号。0.3.5/0.3.6 的已测组合因能力不兼容而双向零请求拒绝。0.3.4 发起路径仍保留整场零请求契约失败，统一部署版本不追溯消除失败。

原 1,228 条加指定的后续 48 条确为 **1,276 条**，仅是两组指定记录之和，不能充作全部历史累计请求数。原 1,228 条包含不同探索/失败/最终批次；本轮新增耐久 330 条也不在其中。原始日志保持不变，关联原始日志清单和修正派生索引的 SHA-256 见 `evidence/existing-evidence-references.json`。

## 交付身份、分歧及剩余项

- 继续使用原包：`C:\Users\infin\AppData\Local\AgentLinkGUI\workspace\freshness-20260910\dist\AgentLink-GUI-0.3.6-Windows-x64.zip`。
- ZIP SHA-256：`265547db9139a2d2d972f8c9472480ffabe01f464fd8c8fcb4f3a968354e9c30`。
- 本轮运行副本每个 app 源文件与原封存哈希一致；新测试为补充验收脚本，未重新打包或替换安装。
- 本轮证据对应：TRACEABILITY.json；逐文件完整性：SHA256SUMS.json / verification.json。

对 B 四项要求无实质分歧，已有证据直接关联，不扩大假设性测试清单。B 独立验证阻塞继续依据其自报，A 未核验 B 的安装或执行环境。

下一步仍是双方各自统一候选并核对启动入口、角色、账号与生效权限，再完成真实双机 SMB 竞争与中断、实际权限隔离、进程硬崩溃与不确定恢复，以及 T03/T04 真实模型/GUI 验收。没有可由 A 本机替代的 B 独立证据；本轮未触碰生产共享、未改变 B 电脑、未替换现用安装、未启动真实模型测试。**完整验收仍未完成。**
